Using C# and EF Core please create a URL shortener with the following specifications:
        - A webpage, on which the user can input a hyperlink
        - The hyperlink should then be stored in a database (SQLITE)
        - A 5 character string should be generated and used as the shortened parameter
        - The webpage outputs the new URL with the domain www.focusmr.eu
        - When the link is accessed, it should redirect to the correct website
